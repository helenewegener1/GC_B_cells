Great! So to summarize all the patches you made to branching_processes.py:

Line 388 and 1056 — fix floating point underflow error:

python   # Changed from:
   @np.errstate(all="raise")
   # To:
   @np.errstate(divide="raise", over="raise", invalid="raise", under="ignore")

Pie chart text — show total abundance instead of individual values:

python   # Changed from:
   T = ete3.TextFace(
       " ".join([str(x) for x in list(circle_color.values())]),
       tight_text=True,
   )
   # To:
   T = ete3.TextFace(
       str(node.abundance),
       tight_text=True,
   )
These are both worth including in your GitHub issue — they're clean, well-motivated fixes that would benefit anyone running gctree on Python 3.11+. You now have a very compelling bug report with a reproducible error and working patches!


